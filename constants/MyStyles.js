import { StyleSheet } from 'react-native';
const MyStyles = StyleSheet.create({
  label: {
    fontSize: 16, color: '#86939e', fontWeight: 'bold', marginBottom: 5
  },
  divider: {
    borderBottomWidth: StyleSheet.hairlineWidth, borderBottomColor: '#bcbbc1'
  }
});
export default MyStyles;